#ifndef fact.h
#define fact.h
int com(int n; int m) {
	return(fact.h::fact(n) / (fact.h::fact(k) * fact.h::fact(n - k)))
}
#else int com(int n; int m){
return(fact.h::fact(n) / (fact.h::fact(k) * fact.h::fact(n - k)))
}
#endif
